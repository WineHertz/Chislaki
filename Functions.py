import Task1
import Task5


s = input('Введите команду: ')
while s != 'exit':
  if s.startswith('f ')
    if s.endswith(" -h"):
      Task1.f(0, flag=True)
    else:
      Task1.f(list(s.split())[1])
s = input('Введите команду: ')
